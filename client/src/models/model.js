export function dataRecord(title,description,image){
    this.title = title;
    this.description = description;
    this.image = image;
}
export function record(id,fields){
    this.id = id;
    this.fields = fields;
}